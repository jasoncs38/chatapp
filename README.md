# Socket-Based Chat Application

A simple Java TCP client–server chat application that supports multiple clients using sockets and multithreading.

## How to Run
Run "ChatServer.java" first, then run "ChatClient.java" in multiple instances.
